# factorybot 优化与待办清单

> 2026-07-14 代码审查产出,覆盖 factorybot agent-service 全部分层(API / 应用 / 领域 / ACL / AI / 编排 / 基础设施 / 测试 / 工程化)。
> 每项含 **位置 / 问题 / 建议**,`[ ]` 勾选即完成。优先级图例见下。
> 路径均相对 `factorybot/`。
> 2026-07-15 补充 7 条增量(编号 25-31):cost 子系统悬空 / checkpointer 未接线 / AI 层健壮性 / 编排抽象一致性等盲点,由三路并行审查核实。
> 2026-07-15 第二轮审查补充 14 条增量(编号 33-46):real 模式 scopes/role 越权、Observability 缺方法致异常路径崩溃、barrier 跨场景复用损坏状态、retry 回路死代码等,由四路并行审查核实并抽验关键 P0。

## 进度总览

| 优先级 | 项数 | 范围 |
|---|---|---|
| 🔴 P0 | 12 | 安全越权链 + 运行时崩溃/静默失败 + cost/checkpointer 盲点 + real 模式 scope 越权/异常路径崩溃 |
| 🟡 P1 | 24 | 可观测性落地 + 领域/功能缺口 + AI 层健壮性 + 编排状态机正确性 |
| 🟢 P2 | 10 | 工程化门禁 + 死代码清理 + 编排抽象一致性 + 防御性/类型安全 |
| 合计 | 46 | |

优先级:🔴 必须修(安全/崩溃) → 🟡 应修(质量/可观测) → 🟢 宜修(工程化/清理)。

---

## 🔴 P0 — 安全 & 运行时正确性

### [x] 1. 租户隔离漏洞链(越权查看/确认编排会话)
- **位置**
  - `app/api/orchestration_router.py:37` `confirm_gate` —— 无 `tenant` 依赖
  - `app/api/orchestration_router.py:48` `get_state` —— 无 `tenant` 依赖
  - `app/api/draft_router.py:26` `GET /draft/{id}/evidence` —— 无 `tenant` 依赖
  - `app/api/schemas.py:68` `ConfirmRequest.user_id = "u_zhang"` —— 默认值可伪造身份
- **问题**:`start` 端点有 `tenant=Depends(tenant_from_headers)`,但 confirm / state / evidence 三端点缺失;叠加 `user_id` 默认 `"u_zhang"`,任意调用方无需认证即可跨租户查看编排状态、确认/驳回 gate、读草稿证据。
- **建议**:三端点统一注入 tenant;service 内(`OrchestrationService.resume` / `get_session`、`DraftService.get_evidence`)按 tenant 校验 session/草稿归属;`user_id` 改必填或从 `X-Tenant-User-Id` header 注入,禁止请求体传入。

### [x] 2. 后台任务异常静默丢失(两处)
- **位置**
  - `app/infrastructure/obs/observability.py:90` `_safe_async`
  - `app/application/orchestration_service.py:119` `create_task(_drive(...))`
- **问题**:`create_task` 后的 `except` 只捕获创建本身异常,task 内部异常(LlmCallLog 写失败、编排驱动失败)完全不可见,生产"瞎飞"。
- **建议**:`add_done_callback` 中取 `t.exception()` 记 CRITICAL 日志;或统一异常回调封装。

### [ ] 3. 确认令牌双写非原子
- **位置**:`app/infrastructure/redis_/confirmation_store.py:62`
- **问题**:`confirm:{token_id}` 与 `confirm:session:{session_id}:{step}` 两次 `setex` 非原子,中途失败破坏"防重复确认"保证。
- **建议**:Lua 脚本/管道原子写;或反转顺序先写 dedup key(token 写失败仅留无害孤儿)。

### [ ] 4. recursion_limit 猴子补丁可能不生效(需确认)
- **位置**:`app/infrastructure/ai/react_graph.py:125`
- **问题**:`compiled.recursion_limit = recursion_limit` 直接设属性,LangGraph 运行时实际通过 `config={"recursion_limit": N}` 读取。若不生效,诊断/编排始终用默认 25,与 config 的 20/40 不符。
- **建议**:改在 `ainvoke` 处传 `config={"recursion_limit": ...}`;或写测试确认当前 LangGraph 版本是否读取该属性。

### [ ] 5. `assert` 做运行时校验,`python -O` 可绕过
- **位置**:`app/infrastructure/acl/base.py:43,60,86`
- **问题**:用 `assert` 检查 `self._fixtures` / `self._http` 是否就绪,`-O` 模式跳过,mock 下静默变 `AttributeError`。
- **建议**:改 `if x is None: raise RuntimeError(...)`。

### [ ] 6. Kafka 消息未反序列化(real 模式必崩,当前为死代码)
- **位置**:`app/infrastructure/kafka/consumer.py:36`
- **问题**:`AIOKafkaConsumer` 未设 `value_deserializer`,`msg.value` 为 bytes,`.get("event_type")` 抛 `AttributeError`。consumer 目前无人调用,启用前必修。
- **建议**:加 `value_deserializer=lambda v: json.loads(v.decode())`。

### [ ] 25. cost 子系统分阶段接入(1/7 ResultCompactor 已接,2026-07-15)
- **位置**:`app/infrastructure/cost/`(CacheControl/EarlyStopDetector/PhaseToolBinder/ResultCompactor/ToolResultCache/ModelRouter.route)
- **问题**:五个组件在 `app/` 下零调用,仅 `cost/__init__.py` re-export 与 tests 引用;`ai/` 目录完全不 import cost;`tool_node.py:92` 注释写"可由 ResultCompactor 压缩"但从未调用;ModelRouter 仅 `validate_on_startup()` 接入,`route()` 死代码。整套 token 成本优化是"设计+文档+零上线"。详见根级总览决策 1。
- **建议**:**需拍板**(决策 1):A 接入主流程(route() 接 llm_factory、ResultCompactor 接 ToolNode、EarlyStop 接 ReAct 终止) / B 文档标注未上线 / C 删除代码+下掉《AgentToken成本优化》设计文档。
- **进度(2026-07-15 第一阶段)**:已采纳"分阶段接入"。ResultCompactor 已接入 ReAct 主流程(react_graph._compact_tool_history 在喂 LLM 前压缩 history tool 消息的 data;state.messages 仍存全文,护栏 _guard_no_evidence 与 trace 读全文不受影响);#15 无白名单工具告警+透传已修。全量 73 测试通过。
- **剩余 6 组件仍悬空**:ModelRouter.route() 待 LLM 单例->多实例改造 + provider 模型映射 + EvalGate 评测数据源就绪;CacheControl 强依赖 anthropic provider;EarlyStop 需 ReAct state 证据计数通道;PhaseToolBinder/ToolResultCache 默认关闭/灰度。后续阶段待 EvalGate 数据源与 provider 目标确定后推进。

### [ ] 26. checkpointer real 模式未接线 SqlSaver(Pod 重启丢会话)
- **位置**:`app/infrastructure/persistence/checkpointer.py:18-29`
- **问题**:real 模式(有 mysql_url 且非 mock)下仍返回 `MemorySaver` 仅打 warn,Pod 重启后所有 interrupt/resume 编排会话(含 gate 确认中断态)丢失。注释了 TODO 但是线上风险。
- **建议**:接 `AsyncSqlSaver` 到 lifespan 初始化;或在部署文档明确标注"当前不支持重启恢复编排会话"。

### [ ] 33. Observability 包装类缺 recursion_limit_hit/schema_error/cost 方法,异常路径 AttributeError 崩溃并吞原始异常(2026-07-15)
- **位置**:`app/infrastructure/obs/observability.py:32-84`(Observability 仅暴露 tool_*/llm_called/low_confidence/session_*);调用方 `app/application/diagnosis_service.py:93` `self._obs.recursion_limit_hit("diagnosis")`(在 `except GraphRecursionError` 内)、`app/infrastructure/ai/observable_chat_model.py:41` `self._obs.schema_error(model)`(在 LLM `except` 内)
- **问题**:`MetricsCollector` 有 `recursion_limit_hit`/`schema_error`/`cost` 三方法,但包装类 `Observability` 未代理。诊断递归超限时进 `except GraphRecursionError` 调 `recursion_limit_hit` -> `AttributeError` 覆盖原始 `GraphRecursionError`,本应降级为 partial report 却崩溃;LLM 异常时同理被 `AttributeError` 吞没。mock 模式不触发故测试假绿,real 模式必崩。比 #11 更严重:#11 只说 `schema_error` 命名误导,实则该方法在包装类上根本不存在。
- **建议**:`Observability` 补 `recursion_limit_hit`/`schema_error`/`cost` 三个代理方法(经 `_safe` 调 `_metrics`),并同步补入 `ObservabilityPort` 协议(见 #39)。

### [x] 34. real 模式租户 scopes/role 回退默认值,跨租户越权 + 隐式提权(2026-07-15)
- **位置(已修)**:`app/api/deps.py:46` `scopes=(tenant_scopes or {}).get(tenant_id, [])`(原 `scopes=default.scopes`);`app/api/deps.py:44` `role=role or "VIEWER"`(原 `role or "ENGINEER"`);配置表 `app/config.py` `tenant_scopes`
- **问题**:`resolve_tenant_context` real 分支用 `default.scopes`(`default = c.default_tenant()` = WS-A 全量读写,含 `rework:write`/`process:write`/`pass:write`),任意 real 租户(如 WS-B)请求都拿到 WS-A 的 scope 集合 -> 跨租户越权写;`X-Tenant-Role` 缺失时默认 `ENGINEER`(高权限)。#1 修了"端点缺 tenant 依赖",但这里在"已解析出的 tenant 上"仍把权限挂到 WS-A,是 #1 残留的权限侧漏洞。
- **建议**(2026-07-15 拍板:采纳配置映射/方案 B):① `deps.py` real 分支 `scopes` 改查配置表 `TENANT_SCOPES: dict[tenant_id, list[str]]`,**未配置租户 `scopes=[]` 默认拒绝写**(安全兜底,取代回退 WS-A 全量);② `role` 缺失回退 `VIEWER`(只读)而非 `ENGINEER`;③ 配置表入 `config.py`/settings,走 git review。per-operator 细粒度门禁仍由 `qualification_check` 独立管,不进 scope 体系;将来需"离职即时吊销"/per-user 动态权限再升级到 MES 权限接口(方案 C)。
- **进度(2026-07-15 已实现)**:`config.py` 增 `tenant_scopes` 配置项(默认 `{}`);`deps.py` real 分支改查配置表(未配置 `scopes=[]` fail-closed)+ role 缺失回退 `VIEWER`;`tenant_from_headers` 透传 `c.settings.tenant_scopes`;`test_tenant_context.py` 补配置表/未配置兜底单测。全量 74 测试通过。**保守默认已内建(WS-A 全权 / WS-B 只读 / 其它 fail-closed);生产按真实车间 ID 用 `TENANT_SCOPES` env 覆盖。**

### [ ] 35. barrier_node 跨场景复用,PROCESS_CHANGE 会话被误置 SUSPENDED + 投递无关动作卡(2026-07-15)
- **位置**:`app/orchestration/code_nodes/barrier.py:33-53`(硬编码读 `tooling_result`/`kitting_result`);`app/orchestration/scenarios/specs.py:179-195`(PROCESS_CHANGE 并行分支为 `draft_sop`+`qualification_check`,汇入同一 `barrier` 节点)
- **问题**:`barrier_node` 是换线场景专用(检查 tooling/kitting 的 PASS/FAIL 分流),但 PROCESS_CHANGE 复用之。该场景 `tooling_result`/`kitting_result` 均为 `{}`,两个 PASS 检查与 FAIL 检查全落空,跌入 kitting FAIL 分支:`dispatcher.push_exception_card(...,"物料齐套未达标,请催料")`(与工艺变更完全无关)+ `repo.update_status(session_id,"SUSPENDED",...)`。虽 spec 中 barrier->gate_sop_publish 为固定边图会继续跑,但 session 已被污染为 SUSPENDED 且动作卡语义错误。
- **建议**:barrier 节点场景参数化,或 PROCESS_CHANGE 用纯同步汇合节点(不做业务判断)。🔴 需拍板:barrier 抽象为通用汇合 + 场景化分流子节点,还是各场景独立 barrier 节点。

### [ ] 36. diagnosis catch-all 把原始异常消息写入报告返回调用方,敏感信息泄漏(2026-07-15)
- **位置**:`app/application/diagnosis_service.py:100` `except Exception as e: report = DiagnosisReport.partial(f"异常: {e}", ...)`
- **问题**:兜底 `except` 把 `str(e)` 直接塞进返回给调用方的 `DiagnosisReport`。异常可能含 DB 连接串、内部路径、SQL、stack 细节,经 API 泄漏给调用方(CWE-209)。
- **建议**:生产环境异常消息脱敏,报告只返通用描述(如"诊断服务内部错误"),原始异常记日志(注意此 catch-all 还会吞掉 #33 的 `AttributeError`,修 #33 后此处仍应脱敏)。

---

## 🟡 P1 — 可观测性落地

### [ ] 7. SESSION_LATENCY 直方图定义未 observe
- **位置**:`app/infrastructure/obs/metrics.py:13`
- **问题**:`SESSION_LATENCY` 注册了但无方法 `.observe()`,会话延迟永远没数据。
- **建议**:加 `session_latency(level, seconds)` 方法,在 `session_finished` 或调用方记录。

### [ ] 8. metrics 端口冲突静默吞掉
- **位置**:`app/infrastructure/obs/metrics.py:97`
- **问题**:`except OSError: pass`,端口被占时零指标暴露且无日志,运维无感知。
- **建议**:至少 log warning;更好走 health check 暴露或 port+1 重试。

### [ ] 9. tool/llm span 缺 tenant/session 属性
- **位置**:`app/infrastructure/obs/tracing.py:32-46`
- **问题**:仅 `session_span` 带 `base_attributes()`,tool/llm span 无法在 Jaeger 按租户/会话过滤。
- **建议**:三类 span 统一 merge `base_attributes()`。

### [ ] 10. 批次脱敏白名单是死代码
- **位置**:`app/infrastructure/obs/redactor.py:31,38-48`
- **问题**:`_BATCH_WHITELIST` 设了从不读,该豁免的批次号仍被脱敏。
- **建议**:`_redact_value` 中先查白名单再脱敏。

### [ ] 11. LLM 观测指标名/值失真(三处)
- **位置**:`app/infrastructure/ai/observable_chat_model.py:41,55`;`app/infrastructure/obs/llm_call_logger.py:22`
- **问题**:`schema_error` 计数器实捕所有 LLM 异常(误导);`completion_tokens=128` 硬编码致结构化输出成本核算失真;`phase` 字段永远空串。
- **建议**:区分 `llm_error` / `schema_error` 计数;结构化输出按 `usage_metadata` 取真实 token 或按 schema 动态估算;phase 经 obs_ctx 透传。

---

## 🟡 P1 — 设计 & 功能缺口

### [ ] 12. OrchestrationState God Object
- **位置**:`app/domain/orchestration_state.py:128`
- **问题**:一个 TypedDict 塞四场景(换线/故障复产/8D/工艺变更)40+ 字段,无场景必填校验。
- **建议**:拆为各场景独立 TypedDict,Union 组合。

### [ ] 13. Draft 贫血模型,版本锚点不变量泄漏
- **位置**:`app/domain/draft.py:17`;`app/application/draft_service.py:38`;`app/application/builders/base.py:71`
- **问题**:"草稿必须继承诊断版本锚点"不变量散在三处。
- **建议**:收进 `Draft.apply_version_anchor(anchor)`,service 只调该方法。

### [ ] 14. 写防线不一致(rework 有双校验,pass/process 没有)
- **位置**:`app/infrastructure/acl/wiring.py:57-59`
- **问题**:`rework_write` 注入 confirmation_store 走 `validate` 双校验,`pass_write` / `process_write` 仅 `valid_for` 弱校验,token 可复用。
- **建议**:三个写 client 统一注入 confirmation_store,或文档化差异并标 `@deprecated`。

### [x] 15. 工具结果压缩默认全量透传(烧 token)
- **位置**:`app/infrastructure/cost/result_compactor.py:36`
- **问题**:未在 `FIELD_WHITELIST` 的工具整包透传给 LLM,大查询结果可能撑爆上下文。
- **建议**:定义默认白名单(仅保留标量顶层键)或无白名单时告警。
- **进度(2026-07-15)**:采纳"无白名单时告警"方案。已接入 ResultCompactor 主流程(见 #25 第一阶段),无白名单工具分支发 `cost.result_compactor.no_whitelist` warning 让缺口可见化,**维持透传不裁剪**(裁剪留待逐工具 FIELD_WHITELIST 领域决策)。token 节省依赖既有白名单 3 工具(pass_records/test_results/traceability_graph)。

### [ ] 16. 工具调用串行,本可并行
- **位置**:`app/infrastructure/ai/tool_node.py:58`
- **问题**:LLM 单次并行 tool_calls 被 `for` 逐个 await。
- **建议**:`asyncio.gather` 并行(注意 trace 写入并发安全)。

### [ ] 17. real 模式断链(两处)
- **位置**:`app/infrastructure/acl/rag.py:59` `fetch_subgraph_nodes` `raise NotImplementedError`;`app/infrastructure/persistence/models.py` ORM 无人引用
- **问题**:DraftService 调 `fetch_subgraph_nodes` 无兜底会 500;ORM 模型定义了但落库路径未接通。
- **建议**:DraftService catch 降级标记 `needs_review`;或在 persistence 接通 `models.py`。

### [ ] 18. ACL HTTP 异常直接传播,上游错误信息丢失
- **位置**:`app/infrastructure/acl/base.py:50,71,93`
- **问题**:`raise_for_status()` 的 `HTTPStatusError` 未包装,上游 MES 业务错误码/详情被丢弃。
- **建议**:catch 后包装为 `AclUpstreamError(status, body)` 抛出。

### [ ] 27. react_graph 用 try/except TypeError 探测 obs_ctx,脆弱(2026-07-15)
- **位置**:`app/infrastructure/ai/react_graph.py:49-54`
- **问题**:`llm.ainvoke(..., obs_ctx=...)` 捕获 TypeError 回退到不带 obs_ctx;真实 LLM 因 tool schema 错误等抛 TypeError 会被误判为"不支持 obs_ctx",静默丢观测上下文。
- **建议**:ObservableChatModel 已统一接受 obs_ctx,直接移除回退分支;或用 `inspect.signature` 探测。

### [ ] 28. MockChatModel 靠 system prompt 子串匹配路由,改 prompt 即崩(2026-07-15)
- **位置**:`app/infrastructure/ai/mock_chat_model.py:30-41`
- **问题**:`if "钢网/程序核对异常" in sys_text` 等硬编码字符串路由;prompt 改标点/措辞 mock 就走错分支,测试假绿。
- **建议**:按 state 中 `capability` 字段路由,不解析 system prompt 文本。

### [ ] 29. SupervisorGraph._map_result 硬编码 agent 输出 key(2026-07-15)
- **位置**:`app/orchestration/supervisor_graph.py:110-122`
- **问题**:用 `"disposition_card" in result` / `"draft_payload" in result` 判 agent 类型;新增 agent 输出格式不同即需在此加分支。
- **建议**:统一 `AgentOutput` 结构(hypothesis/confidence/action_card 等字段),_map_result 通用映射而非按 key 分支。

### [ ] 32. 动作卡投递跨租户误投(assignee 硬编码 u_zhang)(2026-07-15)
- **位置**:`app/application/action_card_dispatcher.py:40,45-46`
- **问题**:`ActionCardDispatcher` 构造默认 `assignee_user_id="u_zhang"`,`_resolve_assignee` 完全忽略 card,所有动作卡(含 WS-B 会话的 draft_payload)都投到 u_zhang 收件箱,跨租户误投 + 信息泄漏。P0 #1 残留(push 侧)。
- **建议**:按会话租户/责任人路由。A 给 `ActionCard` 加 `assignee_user_id` 字段,gate 节点建卡时从 state.tenant 填入;或 B 给 dispatcher 注入 repo,`_resolve_assignee` 按 card.session_id 查 `tenant_context.user_id`。需拍板路由来源。

### [ ] 37. retry_tooling 字段永不写入,gate_disposition retry 回路不可达(死代码)(2026-07-15)
- **位置**:`app/domain/orchestration_state.py:166` `retry_tooling: bool`(仅定义);`app/orchestration/scenarios/specs.py:120` `cond=lambda s: "tooling_check" if s.get("retry_tooling") else "done"`
- **问题**:全代码库无任何节点写 `retry_tooling`,`s.get("retry_tooling")` 恒为 `None`(falsy),条件边恒走 `"done"`,gate_disposition->tooling_check 重试回路永不触发。要么 retry 功能未实现,要么遗漏了写该字段的节点(REJECT 时应置 True)。
- **建议**:实现 retry(gate REJECT 时写 `retry_tooling=True` 并回退 tooling_check)或删除该字段与死分支。

### [ ] 38. GateManager.await_confirmation 静默吞 token 反序列化错误,致无日志 REJECT(2026-07-15)
- **位置**:`app/orchestration/code_nodes/gate.py:32-39` `except (KeyError, TypeError): confirmation = None`
- **问题**:checkpointer 序列化 resume value 为 dict 时用 keyword args 重建 `ConfirmationToken`,字段缺失/类型错被 catch 后 `confirmation=None` -> `decision="REJECT"`,但**零日志**。线上 checkpointer 序列化格式一变,所有 gate 静默 REJECT,排查极困难。
- **建议**:`except` 分支加 `_log.warning("gate.confirmation_token_reconstruct_failed", ...)` 记原始 dict 与异常类型。

### [ ] 39. ObservabilityPort 协议与 Observability/MetricsCollector 实现严重不匹配,DIP 断裂(2026-07-15)
- **位置**:`app/infrastructure/obs/port.py:14-37`(协议仅 6 方法);`app/infrastructure/obs/observability.py:17-128`(实现 9 方法);`app/infrastructure/obs/metrics.py:48-90`(MetricsCollector 另有 3 方法未暴露)
- **问题**:协议 `ObservabilityPort` 只定义 `tool_ok/denied/error`/`llm_called`/`low_confidence`/`session_finished`,实现另有 `session_started`/`session_ended`(被 diagnosis_service 调用)及 #33 的三方法。调用方被迫依赖具体类 `Observability` 而非协议,依赖倒置断裂;也是 #33 方法缺失能逃过类型检查的根因。
- **建议**:把 `session_started`/`session_ended`/`recursion_limit_hit`/`schema_error`/`cost` 补入 `ObservabilityPort`,与 #33 一并修。

### [ ] 40. COST_USD_TOTAL 指标零调用方,LLM 成本永远为 0(2026-07-15)
- **位置**:`app/infrastructure/obs/metrics.py:31,88-90`(定义 + `cost()` 方法);`app/infrastructure/obs/observability.py:52-73`(`llm_called` 不调 `cost()`)
- **问题**:`COST_USD_TOTAL` counter 注册了但 `llm_called()` 不据 token 数+模型定价调 `cost()`,且 `cost()` 未暴露到 `Observability`(见 #33/#39)。Prometheus 中 LLM 费用永远为 0,运维无法感知成本--与 #25 cost 子系统"设计+零上线"同源。
- **建议**:`llm_called` 内按 token 数与模型定价计算并调 `cost()`;`cost()` 暴露到 `Observability`+协议。

### [ ] 41. httpx.AsyncClient real 模式无 close/aclose 生命周期管理(2026-07-15)
- **位置**:`app/infrastructure/acl/wiring.py:40` `httpx.AsyncClient(timeout=3.0)` 注入 14 个 ACL client;`app/infrastructure/acl/base.py`(无 `__del__`/`aclose`/context manager);`build_acl_clients` 返回的 `SimpleNamespace` 无 `close()`
- **问题**:real 模式创建的 AsyncClient 连接池进程生命周期内无关闭路径,app shutdown 时不 graceful close(残留 TCP 连接)。运行期池复用是预期的,但缺 shutdown hook 导致优雅停机时连接未释放。
- **建议**:`build_acl_clients` 返回对象提供 `async def close()` 遍历 client 调 `self._http.aclose()`,接到 `main.py` lifespan 的 shutdown 阶段。

### [ ] 42. draft/orchestration 未知 kind/scenario 抛 ValueError 致 HTTP 500 而非 400(2026-07-15)
- **位置**:`app/application/draft_service.py:33` `raise ValueError(f"无对应草稿生成器: {draft_kind}")`;`app/application/orchestration_service.py:109` `raise ValueError(f"未知场景: {scenario}")`
- **问题**:客户端传入不存在的 draft_kind/scenario,抛裸 `ValueError` 未被异常处理器捕获,返回 500(内部错误)而非 400(参数错误),调用方无法判断是自身参数问题。
- **建议**:改抛 `DomainError`/`ValueError` 子类并在 `main.py` 注册异常处理器映射为 400,或直接 `raise HTTPException(400, ...)`。

### [ ] 43. resume 单/多 interrupt 的 resume_value 序列化不一致,阻碍 #26 SqlSaver 接入(2026-07-15)
- **位置**:`app/application/orchestration_service.py:214-225`
- **问题**:单 interrupt 时 `resume_value = token`(`ConfirmationToken` dataclass 实例),多 interrupt 时 `resume_value[iid] = asdict(tok)`(dict)。checkpointer 切到需 JSON 序列化的后端(如 #26 的 AsyncSqlSaver)时,dataclass 实例可能无法序列化,resume 恢复失败。
- **建议**:统一 `resume_value = asdict(token)`(单/多均 dict)。

### [ ] 44. BaseDraftBuilder.build LLM 调用无异常处理,失败直冲 HTTP 500(2026-07-15)
- **位置**:`app/application/builders/base.py:66` `await self._llm.ainvoke_structured(...)`;`app/application/draft_service.py` `draft()` 亦未捕获
- **问题**:LLM 调用超时/限流/格式错误时异常透传到路由层返回 500,无领域语义。
- **建议**:`build()` 或 `draft()` 加 try/except 转 `DomainError`/降级 partial,与 #42 异常处理器协同。

---

## 🟢 P2 — 工程化门禁

### [ ] 19. 缺根 `.gitignore`
- **位置**:`factorybot/`(无 `.gitignore`)
- **问题**:`.env`(含 API Key)、`__pycache__`、`*.pyc` 未忽略,密钥泄露风险。
- **建议**:加 `.env`、`__pycache__/`、`*.pyc`、`.ruff_cache/`、`.mypy_cache/`、`dist/`。

### [ ] 20. 无 CI / lint / type-check
- **位置**:无 `.github/workflows`;`pyproject.toml` 无 `[tool.ruff]` / `[tool.mypy]`;无 `.pre-commit-config.yaml`
- **建议**:加 ruff + mypy 配置 + pre-commit;`.github/workflows/test.yml` 跑 `uv run pytest`。

### [ ] 21. 测试覆盖缺口
- 零 HTTP 层测试(9 文件全直调 service,无 TestClient)
- 无租户隔离测试(全用 `default_tenant` WS-A)
- 编排 SUSPENDED 失败路径无测试(仅 happy path)
- `code_nodes`(barrier / gate / query_compare)无单测
- **建议**:每 router 加 TestClient 冒烟;双租户隔离断言;构造连续失败断言 SUSPENDED;FailureTracker / GateManager 边界单测。

### [ ] 22. 依赖无上限约束
- **位置**:`pyproject.toml`
- **问题**:全 `>=` 无上限,无 lockfile 克隆可能拉 breaking change。
- **建议**:核心依赖加上限(如 `fastapi>=0.110,<1.0`),文档强调 `uv.lock` 必须提交。

---

## 🟢 P2 — 死代码清理

### [ ] 23. 删除未引用模块
- `app/domain/evidence.py`(`TraceNodeView` / `DocSearchHit` 无人用,实际视图在 `acl/views.py`)
- `app/infrastructure/persistence/models.py`(4 个 ORM 模型无人 import)
- `app/infrastructure/kafka/consumer.py`(`EventSubscriber` 仅 `__init__` re-export,无业务调用)
- `ToolResultCache` 两处(`redis_/tool_cache.py` + `cost/tool_result_cache.py`)无实例化
- `app/orchestration/supervisor_graph.py:22` `_token_to_dict` / `_token_from_dict` 无调用

### [ ] 24. 其他小项
- `app/infrastructure/ai/llm_factory.py:140` `get_llm(obs=None)` 首次调用决定全进程观测行为,解耦单例与 ObservableChatModel 包装
- `app/infrastructure/ai/llm_factory.py:36` 模型名硬编码兜底 `deepseek-chat`,提为配置映射
- `app/orchestration/agents/traceability_agent.py:38` 置信度阈值 0.7 硬编码,与诊断侧统一
- `app/domain/version.py:76` `from_flat` 静默吞非法 `VersionKind`,应 raise
- `README.md` 提及 Neo4j 但 agent-service 无 Neo4j 依赖/代码,移除误导

---

### [ ] 30. build_action_card 两个硬编码字典需同步维护(2026-07-15)
- **位置**:`app/orchestration/action_card_builder.py:15-51`(`STEP_WRITES_VIA` + `intent_map`)
- **问题**:两字典各自维护,新增 gate 步骤需改两处,且与 `ScenarioSpec` 节点名无编译期保证。
- **建议**:合并为 `GateStepConfig` dataclass,ScenarioSpec 引用配置而非字符串 key。

### [ ] 31. barrier_node 是裸函数,其余节点都是类(抽象不一致)(2026-07-15)
- **位置**:`app/orchestration/code_nodes/barrier.py:33` vs `supervisor_graph.py:125`
- **问题**:`barrier_node` 是模块级 async 函数,需 SupervisorGraph 包装;而 GateManager/PlanNode/QueryCompareNodes/WriteViaAppService 均为类,抽象层次不一致。
- **建议**:内聚为 `BarrierNode` 类,与其他节点对齐。

### [ ] 45. 编排节点防御性缺口与死字段(2026-07-15 增量)
- `app/orchestration/code_nodes/query_compare.py:36,44,57,61,62,78,89,114` + `write_via_appservice.py:24,25,33,40,41,48,49,56` 多处 `state["key"]` 硬括号访问,场景装配错误致 key 缺失时抛裸 `KeyError` 无业务语义 -> 改 `state.get()` + 缺失抛 `ValueError("Missing state key: ...")`。
- `query_compare.py:103-105` `bid = state.get("complaint_batch_id") or state.get("batch_id")` 可能为 `None` 直传 ACL -> 加 `if bid is None: return {"supplier_trace_result": {"error": "missing batch identifier"}}`。
- `query_compare.py:107-108` `determine_isolation_scope` 硬编码占位桩恒返 `{"batches": [], "reason": "determined_by_code"}`(隔离范围判定未实现) -> 标 TODO 或实现。
- `app/domain/orchestration_state.py:167` `skip_current_step: bool` 全库无读写,死字段 -> 删或实现。

### [ ] 46. 类型安全/分层/工程小项增量(2026-07-15)
- `app/application/action_card_dispatcher.py:23` `WebSocketManager._inbox` 标注 `dict[str, list[dict]]` 实存 JSON 字符串(`send_to_user` append `model_dump_json()` 的 str),`pop`/`latest` 返回类型 `list[dict]`/`Optional[dict]` 撒谎 -> 改 `dict[str, list[str]]` 或存 dict。
- `app/api/schemas.py:43` `class DraftResponse(Draft): pass` API schema 直接继承领域模型,领域变更泄漏到 API 层;`app/api/draft_router.py:21` `DraftResponse(**draft.model_dump())` 冗余(空子类)-> 定义独立 `DraftResponse(BaseModel)` 显式列字段,直接 `return draft`。
- `app/application/builders/base.py:13` `draft_kind: str  # DraftKind` 标注为 str 实为枚举 -> 改 `draft_kind: DraftKind`。
- `app/api/deps.py:32` `resolve_tenant_context` docstring 称"脱离 FastAPI 便于单测"却抛 `HTTPException`,与单测耦合 -> 抛领域异常,在 `tenant_from_headers` 映射为 HTTPException。
- `app/infrastructure/obs/observability.py:88-92` `_safe` 捕获所有异常 `pass` 零日志,指标埋点失败无感知 -> 至少 `_log.debug`。
- `app/infrastructure/kafka/producer.py:31` `assert self._producer is not None` 同 #5 模式(`-O` 绕过)-> 改 `if None: raise RuntimeError`。
- `app/infrastructure/cost/cache_control.py:29-31` `mark_tools_cache` `t.update(cc.to_dict())` 原地改入参 tools,重复调用累积 cache_control 键(悬空组件,接入即 bug)-> `t = {**t, **cc.to_dict()}`。
- `app/application/diagnosis_service.py:53-110` `DiagnosisSession` 构造+touch 但从不存 repo,函数返回即 GC,整段跟踪死代码 -> 补 `session_repo.save()` 或删。
- `app/orchestration/supervisor_graph.py:117-120` `_map_result` 把含 `draft_payload` 的整个 result 写进 `agent_hypothesis`,与 `action_card.draft_payload` 重复 -> `agent_hypothesis` 排除 `draft_payload`。
- `app/application/draft_service.py:39` `if draft.confidence < 0.5` 硬编码,未跟进诊断 `diagnosis_confidence_threshold` 配置化(同 #24 模式)-> 抽配置项。
- `app/application/orchestration_service.py:274-285` `pending_step()` 公共方法不校验租户(现仅靠 `get_state` 先调 `get_session` 隔离);`orchestration_service.py:263` `session.tenant_context.get("tenant_id")` 键缺失返回 None 致合法租户误拒 -> 加 `tenant` 参数内调 `get_session` + `.get("tenant_id", "")`。
- `app/infrastructure/redis_/fake_redis.py:36` `setex` 接受 TTL=0(真 Redis 报 `invalid expire time`),mock/real 行为不一致;`fake_redis.py:61` `get_redis()` 返回 `Any` 丢类型 -> `if ttl <= 0: raise ValueError` + `-> RedisLike`。
- `app/infrastructure/cost/result_compactor.py:28-29` 非 dict 截断到 200 字符未标 `_truncated`,LLM 无法区分完整 vs 截断 -> 加 `"_truncated": True`。

## 修复建议顺序

1. **先修 P0 #1**(安全链)—— 改动小、收益大,可顺带补租户隔离测试(P2 #21 的一部分)。
2. **再修 P0 #2/#3/#5**(后台异常、双写原子、assert)—— 健壮性,低风险。
3. **P0 #4/#6 需先确认**(recursion_limit 是否生效、consumer 是否启用)再定方案。
4. P1 可观测性块(#7-11)一并做,见效快。
5. P1 设计块(#12-18)按场景迭代,不阻塞。
6. P2 工程化(#19-22)建立门禁后,死代码清理(#23-24)顺手做。
7. **#25 cost 子系统需先拍板(决策 1)** 再定接入或删除;#26 checkpointer real 模式为生产风险,优先于其他 P1。

> 注(2026-07-15 第二轮):新增 P0 #33(Observability 缺方法致 real 模式异常路径崩溃,#39 为其根因需同修)/#34(real 模式 scopes/role 回退默认值越权)优先级仅次于 #1;#35(barrier 跨场景复用)/#36(异常消息泄漏)随健壮性批次;#37 retry 回路死代码、#40 LLM 成本指标为零属功能缺口,按场景迭代。
