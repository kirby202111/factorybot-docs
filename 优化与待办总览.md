# 项目优化与待办总览

> 2026-07-15 三路并行审查(FactoryRAG 代码 / factorybot 代码 / 文档工程结构)产出。
> 本篇只承载**跨项目系统性问题、工程/文档结构、需拍板决策**;各服务代码级待办见子清单。
> 路径相对仓库根。

## 导航

| 范围 | 文档 | 项数 |
|---|---|---|
| factorybot agent-service | [factorybot/优化与待办清单.md](factorybot/优化与待办清单.md) | 31(含 2026-07-15 补充 7 条) |
| FactoryRAG rag-service | [FactoryRAG/优化与待办清单.md](FactoryRAG/优化与待办清单.md) | 15 |
| 跨项目 / 工程化 / 文档 | 本篇 | 7 + 3 决策 |

---

## 🔴 需拍板决策点

以下三项无法自行定夺,标红交还。

### 决策 1. cost 子系统去留
- **背景**:factorybot `app/infrastructure/cost/` 7 组件(CacheControl/EarlyStopDetector/PhaseToolBinder/ResultCompactor/ToolResultCache/ModelRouter.route)经核实**整体悬空**,零接入运行时;而《AgentToken成本优化-设计与实现方案.md》已作为设计/面试话术存在。
- **选项**:A 本周期接入主流程 / B 暂存但文档与面试话术标注"未上线" / C 删除代码 + 下掉设计文档。
- **影响**:直接关系成本优化叙事是否站得住脚。详见 factorybot 清单 #25。

### 决策 2. 跨项目共享内核是否抽取
- **背景**:factorybot 与 FactoryRAG 各自维护几乎相同的 obs/ai(observable_chat_model)/tenant/version(注释自承"镜像 RAG 侧")/kafka/redis_ 基础模块,改一边漏一边是迟早的事。
- **选项**:A 抽 `factorybot-shared` 内核包,两边 pip 依赖 / B 维持镜像 + 加同步检查脚本 / C 不动。
- **影响**:决定后续改 obs/tenant 要改几遍。详见下方"跨项目共享内核未抽取"。

### 决策 3. FactoryRAG 清单落盘 ✅
- **状态**:已决(2026-07-15),照 factorybot 格式生成 [FactoryRAG/优化与待办清单.md](FactoryRAG/优化与待办清单.md)。

---

## 🔴 跨项目系统性问题

### [ ] 1. 跨项目共享内核未抽取(双份事实源)
- **位置**:factorybot `app/infrastructure/{obs,ai,kafka,redis_}` + `app/domain/{version,tenant}.py` ↔ FactoryRAG `app/shared/{obs,ai,kafka,tenant,events/version_contract}`
- **问题**:两套几乎相同的可观测/LLM 包装/租户/版本锚点/消息基础模块各自维护;`factorybot/app/domain/version.py` 注释明确"镜像 RAG 侧 shared/events/version_contract.py"。注释自承"镜像"却未抽取,任一侧改动都不会传导到另一侧。
- **建议**:见决策 2。抽公共包(version_contract/tenant/obs/ai base/kafka/redis_),或至少加镜像一致性检查脚本。

### [ ] 2. route_version 文档残留 40 处需甄别
- **位置**:40 个 .md 仍含 `route_version`(领域模型/面试指南/设计文档/RAG 服务等)
- **问题**:版本锚点通用化后,RAG+agent-service **代码链已对齐** VersionAnchor,但文档未跟。其中图 schema / MES REST / ProcessRouteActivated 事件为**合理保留**的 route 专属,其余多为该清理的残留。
- **建议**:逐一核对,该改改、该删删,避免文档与代码事实源分裂。

### [ ] 3. 根 readme.md 为空,无顶层导航
- **位置**:[readme.md](readme.md)
- **问题**:仓库无顶层说明/导航,新人无从下手;两服务 + 领域模型 + 设计文档 + 面试指南的结构无索引。
- **建议**:补仓库总览(架构图 + 目录指引 + 快速启动 + 文档索引)。

---

## 🟡 工程化门禁

### [ ] 4. 无 CI / 无 lint / 无 type-check
- **位置**:无 `.github/workflows`;两 `pyproject.toml` 均无 `[tool.ruff]`/`[tool.mypy]`;无 `.pre-commit-config.yaml`
- **问题**:无自动化门禁,回归靠人工;factorybot 清单 #20 已记(单服务视角),此处为跨项目统一缺失。
- **建议**:加 ruff + mypy 配置 + pre-commit;`.github/workflows/test.yml` 对两服务各跑 `uv run pytest`。

### [ ] 5. factorybot 无 Dockerfile / docker-compose
- **位置**:`factorybot/`(无;FactoryRAG 已有)
- **问题**:agent-service 无法独立容器化部署,与 rag-service 不对称。
- **建议**:照 FactoryRAG 模式补 Dockerfile + docker-compose(或与 rag-service 合并到根级 compose)。

### [ ] 6. 两项目 Python 版本与构建后端不一致
- **位置**:`FactoryRAG/pyproject.toml`(`>=3.11`,setuptools) ↔ `factorybot/pyproject.toml`(`>=3.12`,hatchling)
- **问题**:版本基线不齐;若抽共享内核(决策 2)需先统一。
- **建议**:统一到 `>=3.12` + 同一构建后端。

### [ ] 7. .gitignore 缺项
- **位置**:[.gitignore](.gitignore)
- **问题**:缺 `.mypy_cache/`、`.ruff_cache/`、IDE 目录(`.vscode/`/`.idea/`);factorybot 子目录无独立 .gitignore(清单 #19)。
- **建议**:补齐缓存/IDE 忽略;factorybot 补子目录 .gitignore 或依赖根级。

---

## 整体修复建议顺序

1. **拍板决策 1(cost 去留)** —— 同时影响 factorybot 代码与成本优化设计文档的公信力。
2. **factorybot P0 + FactoryRAG P0** —— 生产风险优先(httpx 泄漏 / MES 硬编码 / checkpointer 未接线 / 租户越权)。
3. **工程化门禁(#4 CI/lint)** —— 作为所有后续重构的安全网,先立门禁再大改。
4. **决策 2(共享内核)** —— 决定后启动抽取,降低长期维护成本。
5. **文档一致性(#2 route_version / #3 根 readme)** —— 收尾对齐。
