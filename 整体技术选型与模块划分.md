# MES Agent 服务 — 整体技术选型与模块划分

> 本文是 Agent 服务（及其与 RAG 服务、评测库协同）的**总览文档**，基于以下设计文档提炼整合：
> - `AGENT服务/` 下全部 L1/L2/L3 分层与引入路线文档
> - `AGENT服务/` 下三大横切能力文档（Token 成本优化、可观测性、长程任务）
> - `RAG与Agent协同/`、`RAG与Agent评测/` 协同与评测文档
> - `RAG服务/` 引入路线（三路线模块边界）
>
> 目标：一张图看清"选了什么技术、怎么切模块、模块之间怎么集成"。

---

## 1. 整体架构总览

### 1.1 系统全景

系统由四类组件构成，分属两种语言栈：

```
┌─────────────────────────────────────────────────────────────────────┐
│                        前端 / 工位 UI / 责任人卡片                    │
└──────────┬──────────────────────┬──────────────────────────────────┘
           │ R1 直查图             │ R2 诊断 / R3 草稿 / L3 编排
           ▼                      ▼
┌────────────────────┐   ┌──────────────────────────────────────────┐
│   rag-service      │   │              agent-service (Python)        │
│   (Python)         │◄──┤  L1 诊断 │ L2 草稿 │ L3 编排               │
│  A/B/E 三路线      │   │  + 横切：成本 / 可观测 / 长程任务          │
└─────────┬──────────┘   └──────────────┬───────────────────────────┘
          │ Kafka 事件流投影              │ httpx 只读 REST + 受限写
          │ (GraphProjector)             │ + Kafka 事件订阅
          ▼                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│            MES 主体服务 (Java/Spring，已有，不替换)                    │
│   生产执行服务 │ 制造资源服务 │ 设备管理服务 (14 个限界上下文)         │
└─────────────────────────────────────────────────────────────────────┘
          ▲                              ▲
          │      共享底座（跨服务）        │
          └──────── mes-eval 评测库 ──────┘
                  + OTel 可观测后端
```

### 1.2 三大服务边界

| 服务 | 语言 | 职责定位 | 写权限 |
|------|------|---------|--------|
| **agent-service** | Python | 把"查 → 诊断 → 草拟处置 → 推动作"串成自动链；写动作闸门始终在人手里 | L1 全程只读；L2 仅产草稿不落库；L3 受限写 + confirmation gate |
| **rag-service** | Python | 把"工程师手动查 5 个界面"变成一次问答；三路线分线落地 | 全程只读（图是事件流的只读投影） |
| **MES 主体服务** | Java | 过点、工单、工艺、物料、设备等 14 个限界上下文的核心业务与写路径 | 唯一持有业务写权限，聚合根不变式 + 事务发件箱 |
| **mes-eval** | Python（共享库） | RAG 三路线 + Agent 三层级的离线/在线评测、标定、漂移检测、CI 门禁 | 只读评测，不碰生产写路径 |

### 1.3 跨语言协作模型（Python Agent ↔ Java MES）

- **物理隔离即安全边界**：Agent 是只读旁路，最坏情况是"没诊断出来"，不会产生写副作用；跨语言物理边界天然强制 Agent 不进过点主事务、不旁路应用服务写路径。
- **解耦方式**：两边只通过 **REST 只读接口 + Kafka 只读事件**解耦，复用既有契约，不造新管道。
- **链路串联**：Python Agent 调 Java MES REST 时，httpx 的 OTel instrumentation 自动注入 W3C `traceparent` header，Java 侧 OTel agent 续接同一 trace，实现跨语言全链路追踪。
- **取舍理由**：Python 在 AI 生态（LangGraph、模型 SDK、评测工具）上明显更成熟；MES 主体为 Java/Spring 不替换，两边职责正交。

---

## 2. 技术选型总览

### 2.1 编程语言

| 语言 | 版本 | 用途 | 出处 |
|------|------|------|------|
| **Python** | 3.11+ | agent-service / rag-service / mes-eval 主体语言 | L1 实现方案 §2.1 |
| Java | 已有 | MES 三大主体服务（生产执行/制造资源/设备管理），不替换 | L1 实现方案 §1.3 |

### 2.2 Agent / LLM 框架

| 框架/库 | 版本 | 用途 | 出处 |
|---------|------|------|------|
| **LangGraph** | ≥0.2.x（`Command(resume=…)` 语义依赖） | L1 StateGraph ReAct 编排；L3 supervisor + subgraph 编排；interrupt/resume 长程任务 | L1/L3 架构与流程图 §1；长程任务 §3.3 |
| **LangGraph SqlSaver** (`langgraph.checkpoint.mysql`) | — | checkpoint 持久化到 MySQL，进程重启从断点续跑 | 长程任务 §2.1-§2.2 |
| **langchain-core** (`BaseChatModel`) | — | LLM 抽象层，模型可插拔（`llm_factory` 适配各 provider） | L1/L2 实现方案 §2.1 |
| **Pydantic v2** | — | 数据校验、工具入参 schema、结构化输出（`with_structured_output`） | L1/L2/L3 实现方案 §2.1 |

> **不选 AutoGen / CrewAI**：AutoGen conversational 模式不适合严格步骤编排；CrewAI 抽象偏高，难做细粒度权限拦截与 gate 状态机。LangGraph 离原语更近、可控性更强，且与 L1 同构。（L3 实现方案 §2.3）
> **L2 不用 LangGraph**：L2 步骤固定（取证据 → 检索文档 → 综合），无开放规划，用 async 函数编排 + 策略模式更简洁。（L2 实现方案 §2.2）

### 2.3 大模型

| 模型 | 用途 | 出处 |
|------|------|------|
| Claude（如 claude-sonnet-5） | 主力推理模型（L1 诊断 / L2 草拟 / L3 agent 能力 A/B/C/D） | 可观测性 §4.2 |
| 通义千问 | 多 provider 之一，观测一致性验证 | 可观测性 §8.4 |
| DeepSeek | 小模型/便宜模型候选（Router 分类、cascading 降级） | 成本优化 §4.2 |
| Haiku | 便宜分类器候选（L0 入口路由分流） | 成本优化 §4.2 |
| 本地小模型 | Router 分类候选 | 成本优化 §4.2 |
| Claude Opus 级 | LLM-as-judge 裁判模型（v1 用，与被测模型不同族） | 评测 §10.3 |
| Embedding 模型 | 文档型 RAG 向量化（在文档型 RAG 详细设计中定义） | RAG服务引入路线 |

> 全栈**模型可插拔**：通过 `langchain-{provider}` 适配不同供应商，`llm_factory` 统一切换；任何模型降级必须过评测门禁 `EvalGate`。

### 2.4 Web / HTTP / 异步

| 组件 | 用途 | 出处 |
|------|------|------|
| **FastAPI** | HTTP 入口，异步，原生 OpenAPI，与 Pydantic 无缝 | L1/L2/L3 实现方案 §2.1 |
| **httpx**（异步） | ACL 出站 REST（调 MES 上下文 / RAG 服务 / 图服务），自动注入 `traceparent` | L1 §5.5；可观测性 §4.3 |
| **asyncio** | 协程 Task 驱动长程任务、`wait_for` 超时 | 长程任务 §4 |
| **uvicorn + gunicorn worker** | 部署，K8s 独立微服务 `agent-service` | L1 实现方案 §2.1 |

### 2.5 存储

| 组件 | 用途 | 出处 |
|------|------|------|
| **MySQL** | ① SqlSaver checkpoint 三表（`checkpoints`/`checkpoint_writes`/`checkpoint_blobs`）；② 业务可观测表（`tool_call_trace`/`llm_call_log`/`diagnosis_session`/`draft_trace`/`node_trace`/`l3_session` 等）；③ 评测表族（独立 schema `mes_eval`） | 长程任务 §2.2；可观测性 §7.1、§13 |
| **Redis** | ConfirmationStore（token 签发校验，TTL 30min）、工具结果版本化缓存、租户并发限流信号量 | 长程任务 §5；成本优化 §8.1.2 |
| **Neo4j** | 追溯图投影存储（属性图，节点/边/Cypher 查询），由 `GraphProjector` 订阅 Kafka 事件流构建 | GraphRAG协同 §2.1 |
| **向量库** | 文档型 RAG 向量检索（路线 B），在文档型 RAG 详细设计中定义 | RAG服务引入路线 |
| **MinIO** | 对象存储（基础设施，见 `实现说明/基础设施/`） | 实现说明/基础设施 |
| **SQLAlchemy 2.0 (async) + asyncmy** | MySQL ORM | L1 实现方案 §2.1 |

### 2.6 消息中间件

| 组件 | 用途 | 出处 |
|------|------|------|
| **Kafka** (`aiokafka`) | ① 领域事件订阅（`ProcessRouteActivated`/`equipment.fault` 等主动触发）；② L3 动作卡持久推送 topic `agent.action_cards`（离线兜底）；③ RAG 侧 `GraphProjector` 订阅事件流构建图投影 | L1 §2.1；长程任务 §6.1；GraphRAG协同 §1.2 |
| **WebSocket (SSE)** | L3 动作卡实时推送给在线责任人 | L3 实现方案 §2.1 |

### 2.7 可观测组件

| 组件 | 用途 | 出处 |
|------|------|------|
| **OpenTelemetry Python SDK** | trace/span 采集、context 传播 | 可观测性 §4 |
| **OTel Collector** | 接收 Agent + Java 的 trace/metrics，转发后端 | 可观测性 §15.1 |
| **W3C `traceparent`** | 跨语言（Python→Java）链路串联 | 可观测性 §1.4、§4.3 |
| **prometheus-client** | Counter/Histogram/Gauge 指标埋点与暴露 `/metrics` | 可观测性 §5.5 |
| **structlog + JSONRenderer** | 结构化 JSON 日志，自动注入 `trace_id`/`span_id` | 可观测性 §6.1、§6.4 |
| **Tempo / Jaeger** | trace 存储与查询（SRE 火焰图） | 可观测性 §3.2、§15.1 |
| **Prometheus** | 指标存储与告警 | 可观测性 §3.2 |
| **Loki** | 结构化日志存储 | 可观测性 §3.2 |
| **Grafana** | 统一看板（trace/指标/日志/证据链）+ SLI/SLO 告警（P0-P3） | 可观测性 §3.2、§5.4 |
| **PagerDuty / 钉钉** | P0 告警通知（写工具被拦） | 可观测性 §12.2 |

### 2.8 评测组件

| 组件 | 用途 | 出处 |
|------|------|------|
| **pytest** | 评测/回归脚本，`@pytest.mark.parametrize` 喂用例，CI 自动触发 | 评测 §6.4 |
| **Ragas / DeepEval** | 评测框架适配层（忠实度/答案相关性指标），可选 | 评测 §13.1 |
| **Platt scaling**（v1）/ **isotonic regression**（备选） | 置信度标定 | 评测 §8.3 |
| **PSI**（Population Stability Index） | 漂移检测指标 | 评测 §9.2 |
| **Cohen's kappa / Fleiss kappa** | judge 与人工标注一致性 | 评测 §10.4、§11.3 |

### 2.9 其他

| 库/工具 | 用途 | 出处 |
|---------|------|------|
| **pydantic-settings** | 环境变量/配置文件统一管理 | L1 §2.1 |
| **hashlib + secrets** | confirmation token 生成（`token_hex(16)` 32 字符随机 hex） | 长程任务 §5.1 |
| **Celery** | **明确不选**（太重，且 interrupt 是 LangGraph 原语需同进程协程语义） | 长程任务 §4.1 |

---

## 3. 模块划分

### 3.1 `agent-service` 模块结构

agent-service 是单一 Python 微服务，L1/L2/L3 + 三大横切能力共存于同一服务、不同模块，复用 LLM 抽象、ACL 客户端、可观测底座。

```
agent_service/app/
├── api/                          # FastAPI 路由层
│   ├── diagnosis_router.py       # L1: POST /agent/diagnose
│   ├── draft_router.py           # L2: POST /agent/draft, GET /agent/draft/{id}/evidence
│   └── l3_router.py              # L3: POST /agent/l3/{scenario}/start, /confirm
│
├── application/                  # 应用服务层（编排）
│   ├── diagnosis_service.py      # L1: 构建 LangGraph 图、驱动 ReAct 循环
│   ├── draft_service.py          # L2: 分派 + 取证据 + 综合
│   ├── builders/                 # L2 草稿生成器（策略模式）
│   │   ├── rework_order.py       #   返工单草稿
│   │   ├── eight_d.py            #   8D 报告草稿
│   │   └── sop.py                #   SOP 草稿
│   ├── l3_orchestrator.py        # L3: 会话生命周期、驱动 supervisor 图
│   └── action_card_dispatcher.py # L3: 动作卡双通道推送（WS + Kafka）
│
├── domain/                       # Agent 领域模型（DDD）
│   ├── session.py / report.py / tool.py / tenant.py   # L1: DiagnosisSession / DiagnosisReport / ToolRegistry / TenantContext
│   ├── draft.py / evidence.py / gate.py               # L2: Draft / TraceNodeView / NoWriteClientGate
│   └── l3_state.py                                     # L3: L3Session / L3Step / ActionCard / GateDecision
│
├── orchestration/                # L3 编排层（代码层 + agent 能力）
│   ├── supervisor_graph.py       #   supervisor StateGraph（纯代码，不调 LLM）
│   ├── code_nodes/               #   确定性代码节点（不调 LLM）
│   │   ├── plan.py
│   │   ├── query_compare.py      #   钢网/程序/齐套结构化比对
│   │   ├── barrier.py            #   并行分支汇合
│   │   ├── gate.py               #   confirmation gate
│   │   └── write_via_appservice.py
│   ├── agents/                   #   4 类 agent 能力（非确定，调 LLM）
│   │   ├── root_cause_agent.py        # A 根因推理 + 处置卡
│   │   ├── fault_impact_agent.py      # B 故障隔离范围判定
│   │   ├── traceability_agent.py      # C 客诉追溯（嵌入 L1 诊断图）
│   │   └── draft_agents.py            # D SOP/8D/返工工艺草拟
│   └── scenarios/                #   各场景图装配
│       ├── changeover_graph.py         # ① 换线
│       ├── fault_response_graph.py     # ② 设备故障复产
│       ├── complaint_8d_graph.py       # ③ 客诉 8D
│       └── process_change_graph.py     # ④ 工艺变更落地
│
├── infrastructure/               # 基础设施
│   ├── ai/                       # LLM 抽象与图工厂
│   │   ├── graph_builder.py
│   │   ├── llm_factory.py        #   ObservableChatModel 包装任意 BaseChatModel
│   │   └── observable_chat_model.py
│   ├── acl/                      # 防腐层（对 MES 上下文 + RAG）
│   │   ├── pass_execution.py / process_management.py / material.py / device_data.py  # 只读
│   │   ├── tooling.py / equipment_telemetry.py / rework.py                          # L3 受限写
│   │   └── rag.py / doc_rag.py   #   fetch_subgraph_nodes / search_docs
│   ├── kafka/                    # aiokafka 消费与动作卡生产
│   ├── persistence/              # SQLAlchemy 模型 + Repository（含 SqlSaver 三表）
│   ├── redis_/                   # 工具结果缓存 / ConfirmationStore / 限流
│   ├── obs/                      # 【横切·可观测】见 §3.5
│   ├── cost/                     # 【横切·成本】见 §3.5
│   └── longtask/                 # 【横切·长程任务】见 §3.5
│
├── config.py                     # pydantic-settings
└── main.py                       # FastAPI lifespan + 启动断言（红线校验）
```

**对外接口汇总：**

| 端点 | 层级 | 说明 |
|------|------|------|
| `POST /agent/diagnose` | L1 | 发起诊断，返回 `DiagnosisReport`（含 `subgraph_ref`） |
| `POST /agent/draft` | L2 | 草拟处置，返回 `Draft`（`requires_confirmation=True`） |
| `GET /agent/draft/{id}/evidence` | L2 | 回溯草稿证据 |
| `POST /agent/l3/{scenario}/start` | L3 | 启动编排流程（scenario ∈ changeover/fault_response/complaint_8d/process_change） |
| `POST /agent/l3/{session_id}/confirm` | L3 | gate 确认，`Command(resume=token)` 续跑 |

### 3.2 `rag-service` 模块结构（三路线）

rag-service 是**单一 Python 微服务**，A/B/E 三路线作为模块共存于同一服务，共享 LLM/Embedding/可观测/配置/Kafka/ACL/持久化内核；每路线模块自包含（自带 application/domain），路线间调用走 Port/Adapter，保留未来抽成独立微服务的可拆性。三路线分线落地、各自独立演进，最终由 Agentic RAG（路线 E）收口为统一入口。整体结构详见 [`RAG服务/rag-service-整体结构设计.md`](RAG服务/rag-service-整体结构设计.md)。

```
rag_service/app/
├── api/v1/                       # FastAPI 路由层（统一注册）
│   ├── trace_router.py           # A: /rag/trace/query, /rag/trace/expand
│   ├── doc_router.py             # B: /rag/docs/query, /search, /ingest
│   └── chat_router.py            # E: /agent/chat, /agent/explain/{id}
├── routes/                       # 三路线模块（各含 application/domain/infrastructure）
│   ├── traceability/             # A 追溯型（GraphRAG + Neo4j）
│   ├── document/                 # B 文档型（PGVector + MinIO）
│   └── agentic/                  # E Agentic RAG（LangGraph 路由图，L0 收口）
├── shared/                       # 共享内核（Shared Kernel）
│   ├── ai/                       # LlmPort + ObservableChatModel + llm_factory
│   ├── embedding/                # EmbeddingPort + bge-m3 + reranker
│   ├── obs/                      # 可观测底座（context/port/tracing/metrics/logging/redactor）
│   ├── config/                   # BaseSettings + RagSettings（路线级开关）
│   ├── kafka/                    # ConsumerGroup + DomainEvent + 幂等/位点基类
│   ├── acl/                      # BaseReadonlyAclClient + 路线间 Port/Adapter + MesClients
│   ├── persistence/              # 多 Engine 工厂 + Alembic 迁移
│   ├── tenant/                   # TenantContext + 跨服务传递协议
│   ├── web/                      # lifespan + /health /ready + DI 容器
│   └── events/                   # 版本契约（route/bom/rule_version）
├── config.py
└── main.py                       # FastAPI app + lifespan 启动断言 + register_routers
```

**对外接口汇总：**

| 端点 | 路线 | 说明 |
|------|------|------|
| `POST /rag/trace/query` | A | 子图检索 + LLM 综合，返回 `TraceAnswer`（含 `subgraph_ref`） |
| `POST /rag/trace/expand` | A | 只取子图不综合，返回 `TraceSubgraph` |
| `POST /rag/docs/query` | B | 检索 + LLM 综合，返回 `DocAnswer` |
| `POST /rag/docs/search` | B | 只检索 chunks，返回 `list[ChunkHit]` |
| `POST /rag/docs/ingest` | B | 文档摄入（管理接口） |
| `POST /agent/chat` | E | 统一问答入口，返回 `AgentAnswer` |
| `GET /agent/explain/{audit_id}` | E | 回溯路由与工具链 |
| `GET /health` `/ready` `/metrics` | shared | 存活 / 就绪 / prometheus 指标 |

各路线定位与技术形态如下：

| 路线 | 子模块 | 职责 | 技术形态 |
|------|--------|------|---------|
| **A 追溯型** | `RAG服务/追溯型 RAG/` | 全链路追溯，5M1E 根因串联 | GraphRAG + Neo4j Cypher + 领域事件流预投影 |
| **B 文档型** | `RAG服务/文档型 RAG/` | SOP/手册/标准/8D 报告检索 | 向量检索 + 版本过滤 + 事件驱动重索引 |
| **E Agentic RAG** | `RAG服务/Agentic RAG/` | 统一入口，按问题路由到 A/B | 路由 + 工具选择 + 多步推理 |

> 路线 E 依赖 A/B 成型后才做。引入顺序：先 B（易落地验证价值）→ 再 A（护城河）→ E 收口。
>
> **结构要点**：① 路线间禁止直接 import 对方 application/domain，一律走 `shared/acl/` 的 Port（`TraceRagPort`/`DocRagPort`），单服务内注入 InProcess Adapter，拆服务时换 Http Adapter、业务代码零改动；② 统一 `ReadOnly*Gate` 启动断言（投影/摄入/工具三类 gate + 原始数据流订阅 gate）把只读红线变成结构属性；③ 多 DB 共存（Neo4j + PGVector + MySQL + Redis + MinIO），按路线降级、故障域不扩散。
>
> **范围说明**：C 数据型、D 防错即时辅助暂不建设（descoped），不在本文档范围内。

### 3.3 `mes-eval` 评测库模块结构

mes-eval 是与 `rag-service`/`agent-service` 同仓的共享库（monorepo 子模块形态），覆盖 RAG 三路线 + Agent 三层级共 6 个被测对象。

| 组件 | 包路径 | 职责 |
|------|--------|------|
| **EvalRunner** | `mes_eval/application/runner.py` | 离线跑集 + 三层判定（断言/指标/judge） |
| **ShadowRunner** | `mes_eval/application/shadow.py` | 在线影子采样对比，5% 流量复制 |
| **CalibrationService** | `mes_eval/application/calibration.py` | 置信度标定（ECE/Platt/isotonic） |
| **DriftDetector** | `mes_eval/application/drift.py` | 漂移检测（PSI/分布对比） |
| **JudgeService** | `mes_eval/judge/judge_service.py` | LLM-as-judge，按 MES 专用 rubric 判生成质量 |
| **JudgeHumanAligner** | `mes_eval/judge/human_aligner.py` | judge 与人工一致性校准（Cohen's/Fleiss kappa） |
| **MetricsCalculator** | `mes_eval/metrics/calculator.py` | 检索/生成/Agent/安全/校准指标计算 |
| **SafetyChecker** | `mes_eval/metrics/safety.py` | 安全红线硬断言 |
| **VersionAnchorChecker** | `mes_eval/domain/` | 版本锚点强制比对 |
| **CIGate** | `mes_eval/gate/ci_gate.py` | CI 门禁（hard gate 阻断 / soft gate 告警） |
| **EvalTarget 适配器** | `mes_eval/infrastructure/targets/` | 每个被测对象一个适配器（traceability_rag / doc_rag / agentic_rag / agent_l1 / l2 / l3） |

### 3.4 模块间集成点与接口契约

| 集成点 | 接口/契约 | 方向 | 说明 |
|--------|----------|------|------|
| L1 调图 | `query_traceability_graph` 工具（封装 `POST /rag/trace/query`） | Agent → RAG | 注册在 L1 `ToolRegistry` **首位**，system prompt 引导"先调图" |
| L1 降级 REST | 各上下文只读 REST（过点/工艺/物料/质量/设备…） | Agent → MES | 图覆盖不足或需聚合计算时降级补齐 |
| L2 回查图 | `fetch_subgraph_nodes(subgraph_ref)` → `/rag/trace/subgraph/{ref}` | Agent → RAG | L2 按 L1 透传的 `subgraph_ref` 回查图节点，**不重查图** |
| L2 调文档 RAG | `search_docs(query, route_version_filter)` | Agent → RAG | 8D/SOP 草拟时检索历史同类文档 |
| L3 受限写 | ACL 写客户端 → 应用服务 REST（header `X-Confirmation-Token` + `X-Confirmed-By`） | Agent → MES | 走聚合根不变式 + 事务发件箱，与人工下达同路径 |
| 图投影 | Kafka 领域事件流 | MES → RAG | `GraphProjector` 订阅事件构建/更新 Neo4j 图 |
| 主动触发 | `ProcessRouteActivated` / `equipment.fault` 事件 | MES → Agent | 触发 L2 SOP 异步草拟 / L3 故障复产场景 |
| 动作卡推送 | Kafka topic `agent.action_cards` + WebSocket | Agent → 前端 | 双通道，Kafka 兜底离线送达 |
| 评测接入 | EvalTarget 适配器 | mes-eval → Agent/RAG | 每个被测对象一个适配器，统一跑集判定 |
| 跨语言追踪 | W3C `traceparent` header | Agent → MES | httpx OTel instrumentation 自动注入 |

**版本一致性三段传递链**（核心安全契约）：
```
图 SNAPSHOT_OF_ROUTE{route_version}  →  L1 evidence.route_version  →  L2 Draft.route_version  →  MES 应用服务校验 ACTIVE
```

### 3.5 三大横切能力子模块

横切能力作为 `infrastructure/` 下的子包，被 L1/L2/L3 共享复用。

#### 3.5.1 成本优化（`infrastructure/cost/`）

| 组件 | 职责 |
|------|------|
| **ModelRouter** | 按 capability/phase 分派不同规格模型；启动时对每条路由查 `EvalGate.passed`，未过评测则拒绝启动 |
| **EvalGate** | 评测门禁：比对准确率/ECE 阈值，不放行退化模型 |
| **CacheControl** | 为 system prompt + 工具定义标记 `cache_control`（ephemeral, TTL 5min/1h） |
| **PhaseToolBinder** | 按推理阶段动态裁剪工具集（默认关闭，工具集极大时启用） |
| **ResultCompactor** | 工具结果回灌前压缩（字段裁剪 + 列表截断 + 摘要）；模型看摘要，trace 落全文 |
| **EarlyStopDetector** | 冗余检测 + 模型自评 `enough_evidence` → 触发转人工 |
| **ToolResultCache** | 版本化工具结果缓存（Redis），cache key 含 `route_version`/`tenant` |

> 降本五层杠杆（优先级从严到宽）：架构层（代码节点不调 LLM）> 模型层（分层路由）> 提示词层（prompt caching）> 推理循环层（结果压缩/早停）> 缓存层（工具结果缓存）。换便宜模型是**最后一步**，且必须过 `EvalGate`。

#### 3.5.2 可观测性（`infrastructure/obs/`）

| 组件 | 职责 |
|------|------|
| **ObservabilityContext** | 不可变 dataclass，随会话流动（`session_id`/`trace_id`/`tenant`/`level`/`prompt_version`/`step_no`） |
| **ObservabilityPort** (Protocol) | 抽象接口，业务节点依赖它而非 OTel/prometheus 具体实现（DIP） |
| **Tracing** | 封装 OTel span 创建（`session_span`/`tool_span`/`llm_span`） |
| **MetricsCollector** | 所有 Counter/Histogram 集中定义与埋点 |
| **LoggingContext** | structlog 配置，自动注入 `trace_id`/`span_id` |
| **Redactor** | 脱敏纯函数（序列号保留前4后2、物料批次白名单、PII 不采集） |
| **ObservableChatModel** | 包装任意 `BaseChatModel`，统一埋 LLM 观测（token/延迟/模型/`prompt_version`），provider 无关 |
| **LlmCallLogger** | 落 `llm_call_log` 表 |
| **CostEstimator** | 单价表 `model_pricing` × token 估算 USD |
| **Calibration** | 置信度标定曲线（reliability diagram + Platt/isotonic） |
| **EvalRunner** | 离线评测：跑评测集 → 算 accuracy/ECE/evidence_recall → 落 `eval_run` |
| **DriftDetector** | PSI 离线批处理 + 实时突增告警 |

> 五层可观测模型：L0 基础设施层 → L1 链路层（trace/span）→ L2 指标层（prometheus）→ L3 业务视图层（MySQL 平铺证据链）→ L4 评测质量层。Trace **双存储**：Tempo/Jaeger（给 SRE 看火焰图）+ MySQL 平铺表 `tool_call_trace`（给工程师 UI 回溯证据链），同源 trace_id 串联。**观测是只读旁路，失败不反噬业务**。

#### 3.5.3 长程任务（`infrastructure/longtask/`）

| 组件 | 职责 |
|------|------|
| **L3Orchestrator** | 会话生命周期：`start()` 创建 session + `create_task(_drive)` 点火；`resume_suspended()` 恢复挂起会话 |
| **SqlSaver** (LangGraph) | checkpoint 持久化到 MySQL 三表，`thread_id=session_id` |
| **GateManager** | gate 节点：`await_confirmation()` 内调 `interrupt(value=card)` 暂停 + deadline 计时器 + 超时挂起 |
| **ConfirmationStore** (Redis) | token 签发（`token_hex(16)`）、校验（存在性 + action 匹配）、防重复确认，TTL 30min |
| **ActionCardDispatcher** | 双通道推送（WebSocket 实时 + Kafka 兜底），带 `traceparent` |
| **FailureTracker** | 按 session+capability 计数 agent 连续失败，≥2 次 → SUSPENDED |
| **ReworkWriteAclClient** | 写 ACL 客户端：校验 token → POST 应用服务 REST |

> 核心机制：`interrupt(value=card)` 保存 checkpoint 到 MySQL → 抛 `GraphInterrupt`（控制流信号，非错误）→ ainvoke 等待；`Command(resume=token)` 从 MySQL 加载 checkpoint → 反序列化 state → 续跑。Pod 被 OOM Kill 或滚动更新后，新 Pod 只需同一 `thread_id` 调 `ainvoke(Command(resume=…))` 即可跨进程恢复。**不引入 Celery**（太重，且 interrupt 需同进程协程语义）。

---

## 4. Agent 分层架构

### 4.1 自主度光谱（L0-L4，核心落地 L1/L2/L3）

| 层级 | 名称 | 定位 | 写权限 | 风险 |
|------|------|------|--------|------|
| L0 | 收口型问答 Agent | 统一入口路由到 RAG A/B 工具 | 无 | 低 |
| **L1** | **诊断型 Agent** | 多步只读推理，跨上下文串追溯，输出 5M1E 根因假设 | 无（只读 API） | 低 |
| **L2** | **草稿型 Agent** | 生成写意图草稿（返工单/8D/SOP），不落库，人确认后下达 | 生成草稿，不落库 | 中 |
| **L3** | **编排型 Agent** | 跨上下文流程编排，人在回路，受限写 + confirmation gate | 受限写 + gate | 中高 |
| L4 | 自治型 Agent | 无人介入全自动执行 | 全自动写 | 目前不建议 |

### 4.2 层间数据流

```
L1 诊断 → 产出 DiagnosisReport + subgraph_ref
              ↓
L2 草稿 → 消费 L1 输出，按 subgraph_ref 回查图节点 → 产出 Draft（requires_confirmation=True）
              ↓ 人确认（在 MES 正式界面）
          MES 正式应用服务落库（返工/工单/质量上下文，过聚合根不变式 + 事务发件箱）

L3 编排 → supervisor 图驱动代码节点（plan / query+compare / barrier / gate / write）
              ↓ 仅在非确定分支触发
          agent 能力 A/B/C/D（subgraph 调用）→ 产出 ActionCard
              ↓ confirmation gate（interrupt → 人确认 → resume）
          MES 正式应用服务落库
```

### 4.3 L3 "代码 + agent 混合编排" 判定标准

一个步骤是否需要 agent，看三问：
1. **输入是否开放？**
2. **是否需要推理/生成？**
3. **分支是否难以穷举？**

三问皆否 → **代码节点**（不调 LLM）；三问有一 → **agent 节点**。

> 换线全程 PASS 时 agent 节点根本不触发，**LLM 调用为 0**。"代码能做的不交给 LLM"是核心原则——LLM 的非确定性在过点红线附近是负资产。

### 4.4 写动作的三层防线

| 层级 | 防线机制 | 实现 |
|------|---------|------|
| L1 | 全程只读 | `ReadOnlyToolGate` 启动断言，拒绝任何非只读工具 |
| L2 | 不持有任何写 client | `NoWriteClientGate` 启动断言，扫描 ACL client 方法名禁止写动词；`requires_confirmation` 恒 True |
| L3 | 写工具必须声明 `requires_confirmation` + `writes_via` | `WriteToolGate` 启动断言；落库走各上下文正常应用服务过聚合根不变式 + 事务发件箱 |

> Agent 全程不碰 MES 原始表，写路径与正常人工下达完全一致，只是触发源从"人点按钮"变成"Agent 草拟 + 人确认"。

---

## 5. 关键设计决策汇总

| # | 决策 | 核心权衡 | 出处 |
|---|------|---------|------|
| 1 | **用 LangGraph 而非黑盒 AgentExecutor** | StateGraph 把"模型思考 → 工具执行 → 回模型"做成显式图，可对每条边加条件路由/超时/递归上限；`recursion_limit` 是硬上限靠框架兜底 | L1 架构与流程图 §1 |
| 2 | **L3 代码+agent 混合编排** | 代码能做的不交给 LLM；换线 PASS 时 LLM 调用为 0。懂"什么时候不用 AI" | L3 实现方案 §0 |
| 3 | **工具注册对齐限界上下文** | Agent 能调的工具 = 14 个限界上下文暴露的 toolset，边界即工具边界；权限在调用前按 `TenantContext` 过滤 | 引入路线 §4、L1 §4 |
| 4 | **写动作三层 confirmation gate** | L1 只读 / L2 不持写 client / L3 写工具必须声明 + 走应用服务；Agent 不碰原始表 | 引入路线 §4、L2/L3 实现方案 |
| 5 | **图作快路径，REST 作降级** | 图用 `SNAPSHOT_OF_ROUTE` 快照边把版本一致性变成结构属性（物理杜绝失效工艺）；REST 补齐图不擅长的聚合计算 | GraphRAG协同 §2.2 |
| 6 | **L1→L2 靠 subgraph_ref 传证据，L2 不重查图** | L1 透传 `subgraph_ref`，L2 按此回查图节点；L2 风险面最小化，最坏"草稿没用上" | GraphRAG协同 §1.2 |
| 7 | **降本优先级"少调 > 少发 >
 调便宜的"** | 换便宜模型是最后一步且必须过 `EvalGate`；MES 根因准确率影响返工/隔离决策，省钱不能牺牲准确率 | 成本优化 §0、§2.2 |
| 8 | **语义缓存默认关闭** | MES 追溯时变性——"正确答案"随后续数据变化；仅对工艺参数/8D 模板等稳定场景灰度 | 成本优化 §8.2 |
| 9 | **工具结果双路：模型看摘要，trace 落全文** | 降本不牺牲证据链完整性；截断标注 `_truncated`/`_omitted_count` 让模型知道数据不全 | 成本优化 §7.1.2 |
| 10 | **可观测五层 + trace 双存储** | Tempo（SRE 火焰图）+ MySQL 平铺表（工程师 UI 证据链），同源 trace_id；观测是只读旁路失败不反噬业务 | 可观测性 §3.1、§4.5 |
| 11 | **长程任务用 asyncio + LangGraph interrupt/resume，不引入 Celery** | state 在 MySQL 不在进程内存，Pod 重启后同一 `thread_id` 续跑；interrupt 需同进程协程语义 | 长程任务 §4.1 |
| 12 | **评测：安全硬门禁 + 质量软门禁分治** | 安全红线（失效工艺泄漏/写越界/租户越权/过点侵入/PII/实体幻觉/证据空）任一非 0 阻断 CI；质量回归超阈值才阻断 | 评测 §3.2、§6.5 |
| 13 | **版本锚定贯穿评测全程** | 每条金标准用例钉死 `route_version`/`bom_version`/`rule_version` 锚点，`VersionAnchorChecker` 强制比对；取"当前生效版"即判失败 | 评测 §3.2、§4.2 |
| 14 | **跨语言 Python Agent + Java MES** | Agent 是只读旁路最坏"没诊断出来"；Python AI 生态更成熟；物理边界强制不进过点主事务 | L1 §1.3、§2.3 |

---

## 6. 引入路线

| 阶段 | 交付内容 | 依赖 |
|------|---------|------|
| **第一步** | L1 诊断 + L2 草稿 | RAG 路线 A/B 成型后，把追溯检索升级成多步诊断 + 返工单/8D 草稿 |
| **第二步** | 主动巡检叠加 | 订阅已有领域事件做异常预警 |
| **试点** | L3 换线编排 | 挑一条产线做换线 Agent，confirmation gate 做扎实 |
| **收口** | L0 统一 Agent 入口 | 路由到 RAG 工具 + L1-L3 能力 |

RAG 侧引入顺序：B（文档型，易落地）→ A（追溯型，护城河）→ E 收口。

---

## 7. 附录：源文档索引

### Agent 服务
- `AGENT服务/AGENT服务引入路线.md` — 分层定位、引入阶段
- `AGENT服务/L1诊断型Agent/L1诊断型Agent-LangGraph架构与流程图.md` — L1 架构与流程
- `AGENT服务/L1诊断型Agent/L1诊断型Agent-实现方案.md` — L1 实现细节
- `AGENT服务/L2草稿型Agent/L2草稿型Agent-实现方案.md` — L2 实现细节
- `AGENT服务/L3编排型Agent/L3编排型Agent-LangGraph架构与流程图.md` — L3 架构与流程
- `AGENT服务/L3编排型Agent/L3编排型Agent-实现方案.md` — L3 实现细节
- `AGENT服务/L3编排型Agent/L3编排型Agent-痛点操作步骤与解决方案.md` — 痛点与 agent 判定标准
- `AGENT服务/AgentToken成本优化-设计与实现方案.md` — 成本优化
- `AGENT服务/Agent可观测性-设计与实现方案.md` — 可观测性
- `AGENT服务/Agent长程任务-实现细节深度剖析.md` — 长程任务

### RAG 协同与评测
- `RAG与Agent协同/GraphRAG与Agent结合-落地方案.md` — GraphRAG 与 Agent 三层接力
- `RAG与Agent评测/RAG与Agent评测-设计与实现方案.md` — 评测体系
- `RAG服务/RAG服务引入路线.md` — RAG 三路线引入顺序
- `RAG服务/rag-service-整体结构设计.md` — rag-service 单服务三路线模块结构、共享内核、Port/Adapter

### MES 主体（Java，已存在）
- `领域模型/` — 三大服务 14 个限界上下文的事件风暴与领域建模
- `实现说明/` — 业务事件（Outbox/Kafka/MySQL）、基础设施、高频数据方案
